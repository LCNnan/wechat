<!--index.wxml-->
<view class = "map-con">
	<map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="16" style="width: 100%; height: 100%;" show-location controls="{{controls}}" bindcontroltap = "handleControlTap" markers = "{{markers}}"></map>
</view>
<view class = "bottom-area">
	<navigator url = "/pages/publish/publish" class = "publish">发布</navigator>
	<view class = "search">搜索</view>
</view>
